version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - misspell
    - unconvert
    - gocritic
    - gofmt
    - unused

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
